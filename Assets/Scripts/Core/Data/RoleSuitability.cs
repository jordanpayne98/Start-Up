public enum RoleSuitability
{
    Natural     = 4, // ability >= 140
    Accomplished = 3, // ability >= 100
    Competent   = 2, // ability >= 60
    Awkward     = 1, // ability >= 30
    Unsuitable  = 0  // ability < 30
}
